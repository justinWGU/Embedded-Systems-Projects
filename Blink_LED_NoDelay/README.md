# Blink Link LED Without Delay

## This STM32 firmware blinks an LED every second without blocking (using HAL_Delay).
A basic timer peripheral in interrupt mode triggers an event every second to toggle the LED.